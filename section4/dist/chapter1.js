/**
 * 함수 타입 표현식
 */
const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b; // 이렇게 할 수도 있음. 위와 같은 거임
const add2 = (a, b) => a + b;
const sub2 = (a, b) => a - b;
const multiply2 = (a, b) => a * b;
const divide2 = (a, b) => a / b;
// 추가 프로퍼티
add2.name;
export {};
